<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Image Uploader project</title>
    <style>
    body {
        font-family: 'Poppins', sans-serif;
        background: linear-gradient(135deg, #c2e9fb, #a1c4fd); /* light blue gradient */
        color: #ffffff;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        flex-direction: column;
    }

    .container {
        background: #ffffff;
        padding: 40px 30px;
        border-radius: 15px;
        box-shadow: 0 15px 30px rgba(0, 0, 0, 0.1);
        text-align: center;
        max-width: 500px;
        width: 100%;
        animation: fadeIn 1.2s ease-in-out;
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(-30px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    h1 {
        font-size: 36px;
        margin-bottom: 20px;
        background: linear-gradient(90deg, #89f7fe, #66a6ff); /* light cyan to blue */
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        margin-top: 0;
    }

    .form-group {
        width: 100%;
        margin-bottom: 20px;
    }

    input[type="file"] {
        padding: 10px;
        border: none;
        border-radius: 12px;
        background: linear-gradient(145deg, #89f7fe, #66a6ff); /* light blue gradient */
        color: white;
        font-size: 15px;
        cursor: pointer;
        width: 90%;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    input[type="file"]:hover {
        transform: scale(1.05);
        box-shadow: 0 0 15px rgba(102, 166, 255, 0.7); /* blue glow */
    }

    input[type="submit"] {
        padding: 12px 30px;
        background: linear-gradient(135deg, #89f7fe, #66a6ff); /* same as header */
        color: white;
        font-size: 18px;
        font-weight: 600;
        border: none;
        border-radius: 12px;
        cursor: pointer;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        width: 90%;
        margin-top: 15px;
    }

    input[type="submit"]:hover {
        transform: scale(1.05);
        box-shadow: 0 0 20px rgba(102, 166, 255, 0.8); /* hover glow */
    }

    #uploaded-image-container {
        margin-top: 30px;
        width: 100%;
        display: flex;
        justify-content: center;
        flex-direction: column;
        align-items: center;
        color: black;
    }

    img {
        width: 100%;
        height: auto;
        max-width: 300px;
        max-height: 300px;
        border-radius: 10px;
        border: 1px solid #66a6ff;
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
    }

    @media (max-width: 600px) {
        .container {
            padding: 30px 20px;
            width: 90%;
        }

        h1 {
            font-size: 28px;
        }

        input[type="file"],
        input[type="submit"] {
            font-size: 16px;
            padding: 12px;
        }
    }
</style>

</head>
<body>

    <div class="container">
        <form action="/upload" enctype="multipart/form-data" method="POST">
            <div class="form-group">
                <input type="file" name="file" id="file-input">
            </div>
            <div class="form-group">
                <input type="submit" value="Upload">
            </div>
        </form>
    </div>

    <div id="uploaded-image-container">
        <% if(url) {%>
            <h2>File Uploaded Successfully</h2>
            <img src="<%= url %>" alt="uploaded image">
            
            <%} %>
    </div>
    
</body>
</html>